// This file is intentionally disabled to resolve a build conflict.
// The primary Tailwind configuration is located at the project root in `tailwind.config.ts`.
import type {Config} from 'tailwindcss';

const config: Config = {};

export default config;
